#ifndef ENSEIGNANT_H // Vérifie si l'ordinateur n'a pas déjà lu le fichier ENSEIGNANT_H
#define ENSEIGNANT_H // Si ce n'est pas le cas, on le définit maintenant.

#include "qcm_structure.h" // Contient nos structures et les couleurs.

/*
 * Fonction principale du mode enseignant.
 * Appelée depuis le menu principal pour gérer la création des QCM.
 */
void lancerModeEnseignant(); 

#endif

